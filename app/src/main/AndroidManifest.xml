<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".GloomApp"
        android:allowBackup="false"
        android:fullBackupContent="@null"
        android:dataExtractionRules="@null"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GloomhavenHalper">

        <profileable android:shell="true" />

        <activity
            android:name=".screens.MainActivity"
            android:configChanges="locale|layoutDirection|uiMode|screenSize|orientation"
            android:exported="true"
            android:theme="@style/Theme.GloomhavenHalper">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${firebase_crashlytics_enabled}" />

        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="${firebase_analytics_enabled}" />
    </application>

</manifest>
